#include <math.h>
#include <stdio.h>
#include <stdlib.h>
#include <SDL/SDL.h>
#include <math.h>
#include "SDL_gfxPrimitives.h"
#include <SDL/SDL.h>
#include "SDL_ttf.h"
#include "SDL_thread.h"
#include "SDL_mutex.h"

#ifndef MIN
#define MIN(a,b) (((a)<(b)) ? (a) : (b))
#endif
#ifndef MAX
#define MAX(a,b) (((a)>(b)) ? (a) : (b))
#endif
